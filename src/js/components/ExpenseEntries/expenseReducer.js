const defaultState = {
	description: '',
	amount: '',
	lineItems: []
};

export default function ExpenseReducer (state = defaultState, action) {
	// `state = defaultState` is ES6 syntax for defining a default value on a parameter
	return state;
}